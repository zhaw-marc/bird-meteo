version: '3.8'

services:
  bird-meteo:
    build: .
    image: bird-meteo:latest
    ports:
      - "8501:8501"
    volumes:
    # Secrets
      - ./.streamlit:/app/.streamlit
    restart: unless-stopped
